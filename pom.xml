<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <parent>
        <groupId>org.ctoolkit.maven</groupId>
        <artifactId>ctoolkit-parent</artifactId>
        <version>5</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <groupId>org.ctoolkit.migration</groupId>
    <artifactId>ctoolkit-agent-parent</artifactId>
    <packaging>pom</packaging>
    <version>0.2-SNAPSHOT</version>

    <scm>
        <connection>scm:git:[fetch=]git@github.com:turnonline/ctoolkit-agent.git</connection>
        <developerConnection>scm:git:[push=]git@github.com:turnonline/ctoolkit-agent.git</developerConnection>
        <url>https://github.com/turnonline/ctoolkit-agent</url>
        <tag>HEAD</tag>
    </scm>

    <modules>
        <module>ctoolkit-agent-resources</module>
        <module>ctoolkit-agent</module>
    </modules>

    <issueManagement>
        <url>https://github.com/turnonline/ctoolkit-agent/issues</url>
    </issueManagement>

    <developers>
        <developer>
            <name>Aurel Medvegy</name>
            <id>medvegy</id>
            <email>aurel.medvegy@ctoolkit.org</email>
        </developer>
        <developer>
            <name>Jozef Pohorelec</name>
            <id>pohorelec</id>
            <email>jozef.pohorelec@ctoolkit.org</email>
        </developer>
    </developers>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.17</version>
                <configuration>
                    <argLine>-Xmx128m ${jmockit.setting}</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencyManagement>
        <dependencies>
            <!-- Bill Of Material -->
            <dependency>
                <groupId>org.ctoolkit.maven</groupId>
                <artifactId>ctoolkit-project-bom</artifactId>
                <version>${ctoolkit-project-bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.ctoolkit.maven</groupId>
                <artifactId>ctoolkit-library-bom</artifactId>
                <version>${ctoolkit-library-bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <profiles>
        <profile>
            <id>jdk8</id>
            <activation>
                <jdk>1.8</jdk>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <configuration>
                            <additionalparam>-Xdoclint:none</additionalparam>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <properties>
        <gae.version>1.9.48</gae.version>
        <ctoolkit-library-bom.version>3</ctoolkit-library-bom.version>
        <ctoolkit-project-bom.version>1.9.49.2-20170223.084143-1</ctoolkit-project-bom.version>
        <gitkitclient.version>1.2.7</gitkitclient.version>
        <maven-war-plugin.version>2.6</maven-war-plugin.version>
        <!-- Overrides version from bom -->
        <jmockit.version>1.18</jmockit.version>
        <!-- jmockit settings for OpenJDK otherwise throws 'IllegalStateException: To run on OpenJDK 64-Bit Server VM use -javaagent..' -->
        <jmockit.setting>
            -javaagent:${settings.localRepository}/org/jmockit/jmockit/${jmockit.version}/jmockit-${jmockit.version}.jar
        </jmockit.setting>
        <xmlunit.version>1.6</xmlunit.version>
        <gwt.version>2.7.0</gwt.version>
        <guice.version>4.1.0</guice.version>
        <junit.version>4.10</junit.version>
    </properties>

</project>